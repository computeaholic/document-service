version: "3.9"

services:
  db:
    image: postgres:16
    environment:
      POSTGRES_DB: documents
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"

  api:
    build: .
    environment:
      DATABASE_URL: postgresql+psycopg://postgres:postgres@db:5432/documents
    depends_on:
      - db
    ports:
      - "8000:8000"
